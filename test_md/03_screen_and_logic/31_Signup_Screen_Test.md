# Test 31. Signup Screen Functional Test

## 1. 테스트 목적
- 회원가입 신청 워크플로우 및 데이터 검증 로직 확인.

## 2. 테스트 시나리오
| ID | 테스트 항목 | 기대 결과 | 통과 여부 |
| :--- | :--- | :--- | :--- |
| SGN-01 | 중복 아이디 방지 | 이미 존재하는 `user_id` 입력 시 가입 차단 및 `이미 사용 중인 아이디입니다.` 경고 노출 | 통과 |
| SGN-02 | 비밀번호 확인 검증 | `password`와 `passwordConfirm`이 다를 시 Zod 스키마 에러 메시지 즉시 노출 | 통과 |
| SGN-03 | 필수 항목 검증 | 누락된 데이터가 있을 시 가입 버튼 비활성화 또는 에러 메시지 노출 | 통과 |
| SGN-04 | 가입 성공 및 대기 상태 | 정상 신청 완료 시 `users` 테이블에 추가되며 `status`가 기본값인 `대기`로 설정됨 | 통과 |
| SGN-05 | 가입 성공 완료 화면 | 신청 성공 시 신청 완료 컴포넌트 노출 및 로그인 버튼 제공 | 통과 |

## 3. 검증 환경
- 폼 검증 라이브러리: React Hook Form + Zod
- 런타임 오류 방지: 타입스크립트 Any 타입 제거 및 에러 타입(Error) 안전 캐스팅 적용 완료
